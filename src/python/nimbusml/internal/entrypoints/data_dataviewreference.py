# - Generated by tools/entrypoint_compiler.py: do not edit by hand
"""
Data.DataViewReference
"""


from ..utils.entrypoints import EntryPoint
from ..utils.utils import try_set, unlist


def data_dataviewreference(
        data,
        data=None,
        **params):
    """
    **Description**
        Pass dataview from memory to experiment

    :param data: Pointer to IDataView in memory (inputs).
    :param data: The resulting data view (outputs).
    """

    entrypoint_name = 'Data.DataViewReference'
    inputs = {}
    outputs = {}

    if data is not None:
        inputs['Data'] = try_set(
            obj=data,
            none_acceptable=False,
            is_of_type=str)
    if data is not None:
        outputs['Data'] = try_set(
            obj=data,
            none_acceptable=False,
            is_of_type=str)

    input_variables = {
        x for x in unlist(inputs.values())
        if isinstance(x, str) and x.startswith("$")}
    output_variables = {
        x for x in unlist(outputs.values())
        if isinstance(x, str) and x.startswith("$")}

    entrypoint = EntryPoint(
        name=entrypoint_name, inputs=inputs, outputs=outputs,
        input_variables=input_variables,
        output_variables=output_variables)
    return entrypoint
